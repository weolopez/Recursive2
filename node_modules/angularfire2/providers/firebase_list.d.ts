import { Provider } from 'angular2/core';
export interface FirebaseListConfig {
    token?: any;
    path?: string;
}
export declare function FirebaseList(config?: FirebaseListConfig | string): Provider;
export declare function normalizeConfig(config?: FirebaseListConfig | string): FirebaseListConfig;
